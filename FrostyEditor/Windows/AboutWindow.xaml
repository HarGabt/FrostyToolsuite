<ctrl:FrostyDockableWindow x:Class="FrostyEditor.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:ext="clr-namespace:FrostyEditor.Extensions"
        mc:Ignorable="d"
        Title="{DynamicResource fe_About_Title}" Height="450" Width="350"
        ResizeMode="NoResize" Icon="/FrostyCore;component/AppIcon.ico"
        WindowStartupLocation="CenterScreen">

    <ctrl:FrostyWindow.Resources>
        <ctrl:WindowCloseCommand x:Key="CloseCommand"></ctrl:WindowCloseCommand>
    </ctrl:FrostyWindow.Resources>
    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource ListBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <StackPanel Margin="10,4" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Path Data="{Binding Source={StaticResource Icon}, Path=Data}" Stretch="Uniform" Fill="{StaticResource FontColor}" RenderOptions.BitmapScalingMode="HighQuality">
                        <Path.Effect>
                            <DropShadowEffect Opacity="0.8" Direction="0" ShadowDepth="2" />
                        </Path.Effect>
                    </Path>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                        <StackPanel.Effect>
                            <DropShadowEffect Opacity="0.8" Direction="0" ShadowDepth="2" />
                        </StackPanel.Effect>
                        <TextBlock Text="{DynamicResource fe_AppTitle}" Foreground="{StaticResource FontColor}" FontSize="22" FontFamily="{DynamicResource BrandFontFamily}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10,0">
                    <TextBlock x:Name="versionTextBox" Text="Version" Foreground="{StaticResource FontColor}"  HorizontalAlignment="Center" Margin="2,2,2,8" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="{DynamicResource fe_About_Copyright}" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" Margin="2" FontSize="12"/>
                    <TextBlock Text="{DynamicResource fe_About_Authors}" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" Margin="2" FontSize="12"/>
                    <TextBlock Text="{DynamicResource fe_About_OriginalCreator}" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" Margin="2" FontSize="12"/>

                    <Border Background="{StaticResource ControlBackground}" Margin="0,5,0,0">
                        <TextBox Height="158" IsReadOnly="True" Padding="4" Margin="2" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="{DynamicResource MonoFontFamily}" Text="{ext:Text 'Credits.txt'}"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{DynamicResource fe_About_Website}" Foreground="{StaticResource FontColor}" FontWeight="Bold"/>
                        <TextBlock Margin="2, 0, 0, 0">
                            <Hyperlink NavigateUri="https://frostytoolsuite.com" Foreground="{StaticResource FontColor}" RequestNavigate="hyperlink_RequestNavigate">
                                https://frostytoolsuite.com
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{DynamicResource fe_About_Support}" Foreground="{StaticResource FontColor}" FontWeight="Bold"/>
                        <TextBlock Margin="2, 0, 0, 0">
                            <Hyperlink NavigateUri="https://discord.gg/sB8ZUAT" Foreground="{StaticResource FontColor}" RequestNavigate="hyperlink_RequestNavigate">
                                https://discord.gg/sB8ZUAT
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1">
            <Button x:Name="messageBoxOk" Content="{DynamicResource fe_Btn_OK}" Margin="8" HorizontalAlignment="Right" Grid.Row="1" Width="75" Command="{StaticResource CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"/>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
